# Internship evidence screenshots

Capture these only after the repository is published and the public Space is running.

1. **GitHub repository:** open https://github.com/Ahsan-Qamar-Dev/student-dropout-risk-prediction on its Code tab. Include the repository name, Public badge, latest commit, and files (`app.py`, `README.md`, `requirements.txt`, and the four `.pkl` artifacts).
2. **README:** on the same repository, scroll to the rendered README or open `README.md`. Capture the project title, description, and real live application link. A second capture of the Evaluation section is useful if your internship permits extra evidence.
3. **Live application:** open the verified public application link from the README. Include the browser address bar, application title, student inputs, and Predict student risk button.
4. **Final prediction:** use Profile A from the example section (or leave the initial inputs unchanged), click Predict student risk, and capture the visible inputs plus **2.63%**, **Not Dropout**, and **LOW RISK**. Include the address bar so this proves the public app produced the result. Do not use an image of a local app as proof of public deployment.
